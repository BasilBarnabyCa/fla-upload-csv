# Copy this file to .env in the root directory and api/.env
# For Azure Functions, also copy to api/local.settings.json (see api/local.settings.json.example)

# Azure Storage
AZURE_STORAGE_ACCOUNT_NAME=yourstorageaccount
AZURE_STORAGE_ACCOUNT_KEY=yourstoragekey
AZURE_STORAGE_CONTAINER_NAME=csv-uploads

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/fla_uploads?schema=public

# Security - JWT Authentication
JWT_SECRET=your-jwt-secret-here-use-long-random-string
JWT_EXPIRY_HOURS=24

# Users are stored in the database (User table)
# Create users with: node scripts/create-user.js <username> <password>
# Or use the API endpoint: POST /api/users/create (requires authentication)

# CORS
ALLOWED_ORIGINS=https://your-swa-app.azurestaticapps.net,http://localhost:5173

# Upload Limits
MAX_FILE_SIZE_BYTES=10485760
SAS_EXPIRY_MINUTES=10

# Frontend (create .env in root for Vite)
VITE_API_BASE=/api
# No frontend secrets needed - JWT tokens stored in localStorage

